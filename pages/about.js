import React from "react";
// Como tenemos una nueva pagina al navegar a /about ya podemos ver este contenido
// a esto se le llama routing basado en el file system
// NextJs hace routing dependiendo de los que se cree en pages

const About = () => {
  return (
    <div>
      <h1>About</h1>
    </div>
  );
};

export default About;
